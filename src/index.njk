---
title: Hans Bachman
layout: base.njk
---
    <main>
        <section class="hero">
            <div class="hero__aboutwrapper">
                <div class="hero__aboutcont sticky">
                    <h1>Hi, I'm Hans Bachman</h1>
                    <h3><em>3D artist and CEO of BUH</em></h3>
                    <p>I'm a recent undergraduate from the University of Lethbridge in the New Media Program, with a focus on 3D modeling, animation, and web development. </p>
                </div>
                {# <div class="hero__detailcont sticky">
                </div> #}
            </div>
            <div class="hero__featured">
                {# Looping to get all works with "featured" tag #}
                {%-for work in collections.featured-%}

                    <a href="{{work.url}}">
                        <div class="hero__featured__project"  
                        style="background-image: url('{{work.data.imgHighlight}}');
                        background-size: cover;
                        background-repeat: no-repeat;">
                            <div class="hero__featured__project-title">
                                <h2>{{work.data.title}}</h2>
                                <p>{{work.data.typeOfWork | join(' | ')}}</p>
                            </div>
                        </div>
                    </a>

                {%-endfor %}
            </div>
        </section>
        <section class="gallery">
            {# Looping to get works to display in the lower gallery #}
            {%-for work in collections.work-%}

                <div class="gallery__container">
                    <a class="gallery__item" href="{{work.url}}">
                        <div class="gallery__item-title">
                            <h2>{{work.data.title}}</h2>
                            <p>{{work.data.typeOfWork | join(', ')}}</p>
                        </div>
                        <img src="{{work.data.imgHighlight}}" alt="{{work.data.imgHighlightAlt}}">
                    </a>
                </div>

            {%-endfor%}
        </section>
    </main>
    <script src="/JS/scripts.js"></script>
